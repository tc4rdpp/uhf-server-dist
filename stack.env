# UHF Server Configuration
# Server port
PORT=8124

# Host paths for persistent data (relative paths stay out of read-only areas)
DATA_DIR=/opt/uhf-server

# Optional password protection (uncomment to enable)
# PASSWORD=mysecretpassword
